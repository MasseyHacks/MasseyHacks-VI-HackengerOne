<head>
    <link href="stylesheets/bootstrap.css" rel="stylesheet">
    <!--<link href="../public/stylesheets/style.css" type="text/css" rel="stylesheet">-->
    <title>Question 4: Bezos the DMOJ Master</title>
</head>
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/sweetalert/2.1.2/sweetalert.min.js"></script>
<body>
    <div class="container">
        <h1>Question 4: Bezos the DMOJ Master</h1>
        <h4>T<sub>N</sub> = 75025<br>N?<br>Please remeNber Me. DoN’t forget me.</h4>
        <div class="row">
            <div class="col-md-3">
                <img class="img-fluid" src="images/image6.png">
            </div>
            <div class="col-md-3">
                <img class="img-fluid" src="images/image13.png">
            </div>
            <div class="col-md-3">
                <img class="img-fluid" src="images/image15.png">
            </div>
            <div class="col-md-3">
                <img class="img-fluid" src="images/image4.png">
            </div>
        </div>

        <input type="text" name="solution" id="userInp">
        <button type="button" class="btn btn-primary" id="submit">Submit</button>
    </div>
</body>

<script>
    $("#submit").click(() => {
        $.ajax({
            type: "POST",
            url: "api" + window.location.pathname + '/' + btoa($("#userInp").val()),
            success: function (o) {
                if (o !== "wrong") {
                    swal("Good job!").then(() => {
                        window.location.href = o
                    })
                } else {
                    swal("Try again!")
                }
            }
        })
    })
    $("#userInp").keypress(function (e) {
        if (e.which == 13) {
            $("#submit").focus().click()
        }
    })
</script>
